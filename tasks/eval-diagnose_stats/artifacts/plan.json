{
  "tasks": [
    {
      "id": "T-1",
      "description": "Fix the mean() function in stats.py by removing the incorrect +1 from the denominator. The function currently divides by len(numbers) + 1 instead of len(numbers).",
      "files": [
        "stats.py"
      ],
      "depends_on": []
    }
  ]
}
