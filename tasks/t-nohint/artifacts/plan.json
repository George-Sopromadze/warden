{
  "tasks": [
    {
      "id": "T-1",
      "description": "Fix the denominator in the mean() function in stats.py from len(numbers)+1 to len(numbers)",
      "files": [
        "stats.py"
      ],
      "depends_on": []
    }
  ]
}
