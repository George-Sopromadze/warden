{
  "goal": "Fix the bug in the average() function in calculator.py so that it correctly computes the arithmetic mean of a list of numbers and passes all tests in test_calculator.py",
  "scope": [
    "calculator.py: Fix the average() function's division calculation (line 5)",
    "Verify all tests in test_calculator.py pass"
  ],
  "out_of_scope": [
    "Modifying the median() function",
    "Adding new test cases",
    "Changing test_calculator.py",
    "Adding new functions or refactoring beyond the bug fix"
  ],
  "acceptance_criteria": [
    {
      "id": "AC-1",
      "type": "executable",
      "check": "python3 -m pytest test_calculator.py -v",
      "expected": "All tests pass"
    },
    {
      "id": "AC-2",
      "type": "executable",
      "check": "python3 -c \"from calculator import average; assert average([2, 4, 6]) == 4\"",
      "expected": "Assertion succeeds"
    },
    {
      "id": "AC-3",
      "type": "executable",
      "check": "python3 -c \"from calculator import average; assert average([5]) == 5\"",
      "expected": "Assertion succeeds"
    },
    {
      "id": "AC-4",
      "type": "judgment",
      "check": "Does the median() function remain unchanged?",
      "expected": "Yes"
    }
  ]
}
