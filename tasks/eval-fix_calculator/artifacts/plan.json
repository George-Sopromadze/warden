{
  "tasks": [
    {
      "id": "T-1",
      "description": "Fix the average() function by correcting the division to use len(numbers) instead of len(numbers) - 1",
      "files": [
        "calculator.py"
      ],
      "depends_on": []
    }
  ]
}
