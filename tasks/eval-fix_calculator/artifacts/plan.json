{
  "tasks": [
    {
      "id": "T-1",
      "description": "Fix the average() function by changing the divisor from len(numbers) - 1 to len(numbers) on line 5",
      "files": [
        "calculator.py"
      ],
      "depends_on": []
    }
  ]
}
