{
  "tasks": [
    {
      "id": "T-1",
      "description": "Fix the average() function by changing the denominator from (len(numbers) - 1) to len(numbers)",
      "files": [
        "calculator.py"
      ],
      "depends_on": []
    }
  ],
  "flags": []
}
